# Firebase Configuration
# Replace the placeholder values with your actual Firebase project credentials.
# You can find these values in the Firebase Console under Project Settings > General > Your apps.

# Firebase API Key
# This is a unique identifier for your Firebase project.
VITE_FIREBASE_API_KEY=your_api_key

# Firebase Auth Domain
# The domain for Firebase Authentication (e.g., your-app.firebaseapp.com).
VITE_FIREBASE_AUTH_DOMAIN=your_auth_domain

# Firebase Project ID
# The unique identifier for your Firebase project.
VITE_FIREBASE_PROJECT_ID=your_project_id

# Firebase Storage Bucket
# The Google Cloud Storage bucket for your Firebase project (e.g., your-app.appspot.com).
VITE_FIREBASE_STORAGE_BUCKET=your_storage_bucket

# Firebase Messaging Sender ID
# The sender ID for Firebase Cloud Messaging (used for push notifications).
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id

# Firebase App ID
# The unique identifier for your Firebase app.
VITE_FIREBASE_APP_ID=your_app_id